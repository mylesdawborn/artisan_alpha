import 'package:flutter/material.dart';

class PostingPage extends StatelessWidget {

 @override
 Widget build(BuildContext context) {
   return Center(
     child: Container(
       child: Text('Welcome to posting'),
     ),
   );
 }
}